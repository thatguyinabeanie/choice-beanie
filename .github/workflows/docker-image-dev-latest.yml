name: Dev Docker Image

on:
  push:
    branches:
      - main
    paths:
      - BASE_IMAGE_VERSION

jobs:
  build-docker:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write
      id-token: write

    steps:
      - name: Setup QEMU
        uses: docker/setup-qemu-action@v3
        with:
          platforms: all

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Log in to GitHub Packages
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract version from file
        id: extract_version
        run: |
          VERSION=$(cat BASE_IMAGE_VERSION)
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          MAJOR=$(echo $VERSION | cut -d'.' -f1)
          MINOR=$(echo $VERSION | cut -d'.' -f2)
          PATCH=$(echo $VERSION | cut -d'.' -f3)
          echo "MAJOR=$MAJOR" >> $GITHUB_ENV
          echo "MINOR=$MAJOR.$MINOR" >> $GITHUB_ENV
          echo "PATCH=$MAJOR.$MINOR.$PATCH" >> $GITHUB_ENV

      - name: Build & Push Docker Image
        if: github.ref == 'refs/heads/main' || github.event_name == 'release'
        uses: docker/build-push-action@v6
        with:
          target: development
          platforms: linux/amd64,linux/arm64
          push: true
          cache-from: type=registry,ref=${{ github.repository }}:buildcache
          cache-to: type=registry,ref=${{ github.repository }}:buildcache,mode=max
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/battle-stadium:dev-latest
            ${{ secrets.DOCKER_USERNAME }}/battle-stadium:dev-${{env.MAJOR}}
            ${{ secrets.DOCKER_USERNAME }}/battle-stadium:dev-${{env.MINOR}}
            ${{ secrets.DOCKER_USERNAME }}/battle-stadium:dev-${{env.PATCH}}
            ghcr.io/${{ github.repository }}:dev-latest
            ghcr.io/${{ secrets.DOCKER_USERNAME }}/battle-stadium:dev-${{env.MAJOR}}
            ghcr.io/${{ secrets.DOCKER_USERNAME }}/battle-stadium:dev-${{env.MINOR}}
            ghcr.io/${{ secrets.DOCKER_USERNAME }}/battle-stadium:dev-${{env.PATCH}}
